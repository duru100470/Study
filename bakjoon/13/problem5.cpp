#include <bits/stdc++.h>

using namespace std;

int main(int argc, const char **argv)
{
    int R;
    cin >> R;
    cout<<fixed;
    cout.precision(6);
    double taxi = (double)(R*R + R*R);
    cout << R * R * M_PI << endl << taxi << endl;
    return 0;
}